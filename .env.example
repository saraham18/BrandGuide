# Copy to .env and fill in. .env is gitignored — your key never gets committed.

# Required: Claude key used to synthesize the brand guide.
ANTHROPIC_API_KEY=
# Optional: override the model (default: claude-sonnet-4-6)
BRANDFORGE_MODEL=claude-sonnet-4-6
